import { StyleSheet } from 'react-native';

export const segmentStyle = StyleSheet.create({
    all: {
        width: '50%',
        alignSelf: 'center',
        marginTop: 15,
        marginBottom: 15,
    },
    map: {
        position: 'absolute',
        zIndex: 1000,
    },
    list: {

    },
});
