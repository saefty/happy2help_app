import { StyleSheet } from 'react-native';

export const segmentStyle = StyleSheet.create({
    list: {
        width: '50%',
        alignSelf: 'center',
        marginTop: 20,
    },
    map: {
        position: 'absolute',
        zIndex: 1000,
    },
});
