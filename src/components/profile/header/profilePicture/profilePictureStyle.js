// @flow
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    profilePicture: {
        width: 110,
        height: 110,
        borderRadius: 150,
        margin: 10,
        borderWidth: 8,
        borderColor: 'white',
    },
});
