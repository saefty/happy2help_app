/* @flow */

import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    title: {
        width: '100%',
        fontSize: 30,
        marginTop: 15,
        textAlign: 'center',
    },
    subTitle: {
        width: '100%',
        fontSize: 10,
        textAlign: 'center',
        padding: 10,
    },
    modal: {
        justifyContent: 'center',
        alignItems: 'center',
    },
    modalView: {
        width: '80%',
        backgroundColor: '#fff',
        borderRadius: 15,
        elevation: 6,
        alignItems: 'center',
        justifyContent: 'center',
    },
});
