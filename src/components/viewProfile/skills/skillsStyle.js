// @flow
import { StyleSheet } from 'react-native';


export const styles = StyleSheet.create({
    chipBox: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        alignItems: 'flex-start',
    },
    title: { margin: 5 },
    container: { margin: 10 }

});
